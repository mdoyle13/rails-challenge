<% if resource.errors.any? %>
<div>
  <h2><%= pluralize(resource.errors.count, "error") %> encountered:</h2>
  <ul>
  <% resource.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
